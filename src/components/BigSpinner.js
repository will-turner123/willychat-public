export default function BigSpinner(color="text-secondary"){

    return(
        <div class="spinner-wrapper text-center mx-auto my-auto">
            <div class={"big-spinner " + color}/>
        </div>
    )
}